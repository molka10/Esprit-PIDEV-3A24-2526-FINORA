<form method="post" action="{{ path('app_investment_delete', {'investmentId': investment.investmentId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ investment.investmentId) }}">
    <button class="btn">Delete</button>
</form>
