{% extends 'base.html.twig' %}

{% block title %}Edit Investment Management{% endblock %}

{% block body %}

<div class="container mt-4">

    <h1 class="mb-4">✏️ Edit Investment Management</h1>

    <div class="card shadow p-4">

        {{ include('investment_management/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="mt-3 d-flex gap-2">

            <a class="btn btn-secondary" href="{{ path('app_management_index') }}">
                ⬅ Back
            </a>

            {{ include('investment_management/_delete_form.html.twig', {'item': item}) }}

        </div>

    </div>

</div>

{% endblock %}